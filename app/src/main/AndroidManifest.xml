<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location permissions (for future use) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Phone call permission -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    
    <!-- File access permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectPRM392"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDBUOW4v71Ww_Z6xBWY8Emc_3wGX2fbnvU" />
        
        <activity
            android:name=".activities.ApplicationActivity"
            android:exported="false" />
        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- HomeActivity -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" /> <!-- LoginActivity -->
        <activity
            android:name=".views.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" /> <!-- RegisterActivity -->
        <activity
            android:name=".views.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- ReportActivity -->
        <activity
            android:name=".activities.ReportActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>

        <!-- PostHistoryActivity -->
        <activity
            android:name=".activities.PostHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392">
            <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>

        <!-- Post Type Selection Activity -->
        <activity
            android:name=".activities.PostTypeSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- Create Job Seeking Activity -->
        <activity
            android:name=".activities.CreateJobSeekingActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- Map Location Picker Activity -->
        <activity
            android:name=".activities.MapLocationPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- Create Job Posting Activity -->
        <activity
            android:name=".activities.CreateJobPostingActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
        
        <!-- Profile Activity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- Job Detail Activity -->
        <activity
            android:name=".activities.JobDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <!-- Application Form Activity -->
        <activity
            android:name=".activities.ApplicationFormActivity"/>
        
        <!-- EmailVerificationActivity -->
        <activity
            android:name=".views.EmailVerificationActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />

        <activity android:name=".activities.MyApplicationActivity"

            android:exported="false"

            android:theme="@style/Theme.ProjectPRM392"/>
            
        <!-- ForgotPasswordActivity -->
        <activity
            android:name=".views.ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- ResetPasswordActivity -->
        <activity
            android:name=".views.ResetPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Admin Dashboard Activity -->
        <activity
            android:name=".activities.admin.AdminDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Admin User Management Activity -->
        <activity
            android:name=".activities.admin.AdminUserManagementActivity"
            android:exported="false"
            android:parentActivityName=".activities.admin.AdminDashboardActivity"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Admin Post Management Activity -->
        <activity
            android:name=".activities.admin.AdminPostManagementActivity"
            android:exported="false"
            android:parentActivityName=".activities.admin.AdminDashboardActivity"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Admin Report Management Activity -->
        <activity
            android:name=".activities.admin.AdminReportManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Admin Application Management Activity -->
<!--        <activity-->
<!--            android:name=".activities.admin.AdminApplicationManagementActivity"/>-->

        <!-- ApprovedUsersActivity -->
        <activity
            android:name=".activities.ApprovedUsersActivity"/>
        
        <!-- ChatActivity -->
        <activity
            android:name=".activities.ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
        
        <!-- ConversationListActivity -->
        <activity
            android:name=".activities.ConversationListActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <!-- Payment Activities -->
        <activity
            android:name=".activities.PaymentActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
            
        <activity
            android:name=".activities.PaymentSuccessActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjectPRM392" />
    </application>

</manifest>